---
import { ButtonStar, SectionBigTitle } from '@/ui'

import HomeProjectCard from './HomeProjectCard.astro'

const PROJECTS_LINKS = [
  { name: 'Arkanoid', slug: 'arkanoid' },
  { name: 'Api Geo IP', slug: 'api-geo-ip' },
  { name: 'Tetris', slug: 'tetris' },
  { name: 'Tiermaker', slug: 'tiermaker' },
  { name: 'Tinder', slug: 'tinder' },
]
---

<SectionBigTitle title="Proyectos" class="mt-28 md:mt-96">
  <div class="container mx-auto grid grid-cols-1 lg:grid-cols-2 xl:grid-cols-3 gap-4 px-4 md:px-0">
    {
      PROJECTS_LINKS.map((project, index) => (
        <HomeProjectCard
          {...project}
          description="Lorem ipsum dolor sit amet consectetur adipisicing elit. Quisquam, quos."
          image={`/images/${project.slug}/screenshot.png`}
        />
      ))
    }
  </div>
  <div class="flex justify-center mt-20">
    <ButtonStar> Ver más </ButtonStar>
  </div>
</SectionBigTitle>
